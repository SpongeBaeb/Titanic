# 🚢 개발 로그 (DEV_LOG)

## 📅 [2026-07-31] MVP Phase 1 (Stateless E2E 파이프라인) 개발 완료

### 🎯 목표
- Next.js 기반 프론트엔드 환경 및 퍼널(Funnel) UI 구현
- Flask 기반 API 스캐폴딩 및 XGBoost 머신러닝 모델 학습
- `Titanic-Dataset.csv` 데이터를 활용한 역사적 인물 매칭 및 통계적 생존율 예측 연동
- 시네마틱 애니메이션 기반의 3단계 결과 화면(Result Reveal Sequence) 개발

### 🛠️ 주요 진행 사항 (구현 완료)
- `PROJECT_RULES.md` 생성 및 기획 가이드라인 정립 완료.
- 프론트엔드: Zustand 기반 상태 관리 스토어(`quizStore.ts`) 및 Q0~Q7 퍼널 라우팅 100% 구현.
- 프론트엔드: `framer-motion`을 활용하여 페르소나와 통계 확률이 3단계에 걸쳐 교차 등장하는 `ResultView.tsx` 개발 및 연동 완료.
- 프론트엔드: 성별(Sex) 예측의 정확도를 위해 프롤로그 직후 성별을 묻는 `Q0` 문항 UI 선제적 추가.
- 백엔드: `scikit-learn`, `XGBoost`를 이용하여 `Titanic-Dataset.csv`를 읽고 생존 여부를 학습한 모델(`model.joblib`) 파이프라인(`train.py`) 완성.
- 백엔드: 기획서 가이드라인(UI-UX Guide)을 완벽 준수하여 **30인 MVP 캐릭터 DB**(`characters.json`) 구축. 
- 백엔드: `historical_matcher.py` 로직을 **Hard Filter(성별, 나이)**와 **Similarity Score(객실, 요금, 가족 수)** 알고리즘으로 전면 개편하여 가장 합리적인 역사적 캐릭터를 매칭하도록 개선 완료.
- 백엔드: 프론트엔드에서 보낸 퀴즈 데이터를 받아 머신러닝 예측 -> 행동 보정 -> 역사적 인물 매칭까지 한 번에 처리하는 최종 통합 API(`/api/quiz/submit`) 연동 성공.

### ❓ Blockers & Ambiguities
- (해결됨) 30인 MVP 캐릭터 매칭 시 처음엔 사용자의 페르소나를 우선하여 매칭했으나, 기획서의 의도("인구통계학적 Feature Similarity를 우선한다")에 맞게 로직을 전면 수정했습니다.
- (해결됨) 프론트엔드 ResultView 컴포넌트가 렌더링되지 않던 버그를 정적 컴포넌트 중복 코드를 제거하여 해결했습니다.

---

## 📅 [2026-08-02] Phase 2 (결과 공유 및 몰입감 극대화 UI 적용) 완료

### 🎯 목표
- 백엔드 DB 연동(SQLite/Supabase) 및 결과 공유 기능 구현
- 시네마틱한 3D 플립 카드 UI(TitanicPassengerCard) 적용
- 사용자 경험(UX) 극대화를 위한 다중 매칭(Worst, Opposite) 로직 및 애니메이션 효과 구현

### 🛠️ 주요 진행 사항 (구현 완료)
- **공유 및 이미지 저장 기능**: `ResultView.tsx` 하단에 링크 복사, 이미지 저장(`html2canvas` 활용), 카카오톡 공유 기능 구현 완료.
- **백엔드 DB 구축**: `backend/engine/db.py`를 통해 UUID 기반의 퀴즈 결과 저장 기능(SQLite 및 Supabase Fallback) 연동 완료.
- **신규 캐릭터 카드 UI 도입**: 기존 텍스트 형태의 카드를 Tailwind CSS 3D Transform을 활용한 양면 플립 카드(`TitanicPassengerCard`)로 전면 개편.
- **마크다운 기반 캐릭터 DB 이식**: 기획서 마크다운 파일(`titanic_character_cards.md`)을 파이썬 스크립트 기반으로 파싱하여 프론트엔드 전용 `characterDB.ts` 데이터 소스 구축.
- **동적 스탯 연동**: 백엔드에서 전달된 인물 이름과 매칭율(`matchRate`)을 바탕으로 카드 앞면(뱃지, 인적사항)과 뒷면(Recharts 레이더 차트)에 실제 캐릭터 정보 동적 반영. 성별에 따른 기본 초상화 이미지 분기 처리.
- **대체 매칭 인물 스위칭 기능**: 매칭 로직(`historical_matcher.py`)을 확장하여 **최고 유사도(Best)**, **최저 유사도(Worst)**, **다른 성별(Opposite)** 3명의 결과를 한 번에 계산하고 반환하도록 고도화. 결과 화면에서 페이지 새로고침 없이 텍스트 버튼으로 즉각적인 3D 카드 교체 상태값 구현.
- **몰입형 백그라운드 이미지 연출**: 연령대 선택 문항 및 동반자 선택 문항(`questions.ts`) 호버 시, 화면 뒷배경에 시네마틱하게 이미지가 띄워지는 `QuestionView` 애니메이션 연출 완벽 적용.
- **기타 개선**: 프롤로그 화면에 개발용 디버깅(결과창 직행) 버튼 추가 및 결과 화면 하단에 AI 창작물 명시(Disclaimer) 안내문 삽입.

### ❓ Blockers & Ambiguities
- (해결됨) 3가지 다중 매칭 결과를 반환하도록 백엔드를 수정하면서 DB의 하위 호환성 문제가 우려되었으나, `historical_match` JSON 컬럼에 유연하게 딕셔너리(`{best, worst, opposite}`)로 래핑하여 DB 마이그레이션 없이 매끄럽게 처리했습니다.
- (해결됨) 초기에는 인구통계학적 유사도(Feature Similarity)만을 기준으로 매칭했으나, 심리적 페르소나(Persona)와 인구통계학적 특징을 모두 종합적으로 고려하는 하이브리드 가중치 알고리즘(페르소나 불일치 시 페널티 부여)으로 최종 개편했습니다.
- (수정) 답변 버튼 호버 시 안에서부터 차오르는 게이지 애니메이션을 제거하여 시각적 복잡도를 낮추고, 텍스트와 테두리 하이라이트 효과만 유지하여 심플하고 세련된 UI로 정제했습니다.
- (수정) 3페이지부터 적용되는 항구 배경 이미지가 파일명 오류(`bg-dock.png`)로 노출되지 않던 문제를 수정했습니다.

---

## 📌 남은 할 일 (앞으로 구현해야 할 Next Steps)

### 🎨 1. UI/UX 디자인 폴리싱 및 모바일 최적화
- [ ] 모바일 환경에 맞춘 완벽한 반응형(Responsive) 뷰포트 최적화
- [ ] 기획서에 맞춘 세밀한 컬러 팔레트 및 타이포그래피(폰트) 다듬기
- [ ] `sound.ts`에 실제 음원 에셋(클릭, 심장 박동 소리 등) 적용

### 📊 2. 글로벌 유저 통계 기능 (Phase 3 준비)
- [ ] 전체 유저 통계 비교 기능 제공 ("참여자의 X%가 당신과 같은 선택을 했습니다")
